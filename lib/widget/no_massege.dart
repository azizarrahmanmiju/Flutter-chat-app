import 'package:flutter/material.dart';

class NoMassege extends StatelessWidget {
  const NoMassege({
    Key? key,
  });
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text(
          'No Massege',
        ),
      ),
    );
  }
}
